ori $s0,$0,0x7f00    //将定时器中寄存器的基地址放入$s0
ori $s1,$0,0x7f10    //输出设备
ori $s2,$0,0x7f20    //输入设备   
lw $t0,($s2)         //读取输入设备数据存入$t0
sw $t0,($s1)        //$t0数据写入输出设备中寄存器temp1
sw $t0,4($s1)       //$t0数据写入输出设备中寄存器temp2
ori $t1,$0,0x0401   //将0x0401放入$t1
mtc0 $t1,$12          //给SR赋初值   
mfc0 $s3,$15           //将prid的值放入$s3
ori $t2,$0,10         //将$t2赋值10
sw $t2,4($s0)         //给preset寄存器赋初值
ori $t3,$0,9         
sw $t3,($s0)         //给ctrl寄存器赋初值
loop:j loop          //进入死循环等待中断信号产生
